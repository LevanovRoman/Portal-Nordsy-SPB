package com.myapp.portalnordsyspb.suggestionForImprovement.repository;

import com.myapp.portalnordsyspb.suggestionForImprovement.entity.SuggestionDirector;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SuggestionDirectorRepository extends JpaRepository<SuggestionDirector, Long> {
}
